Algoritmo "semnome"
// 

Var

  a, b, resultado : inteiro

Inicio

 a <- 5
 b <- 2
 resultado <- a / b   //ERRO, a solução é trocar a "/" por "\" porque a "/" só é utilizada para valores reais.
 escreval(resultado)


Fimalgoritmo